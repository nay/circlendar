<div class="w-full max-w-2xl mx-auto">
  <div class="mb-4">
    <%= link_to "← #{Venue.model_name.human}一覧に戻る", admin_venues_path, class: "text-blue-600 hover:text-blue-700 text-sm" %>
  </div>
  <h1 class="text-3xl font-bold text-gray-900 mb-8"><%= Venue.model_name.human %>を作成</h1>

  <%= render "form", venue: @venue %>
</div>
