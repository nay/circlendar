<div class="w-full max-w-2xl mx-auto">
  <div class="mb-4">
    <%= link_to "← お知らせ詳細に戻る", admin_announcement_path(@announcement), class: "text-blue-600 hover:text-blue-700 text-sm" %>
  </div>
  <h1 class="text-3xl font-bold text-gray-900 mb-8"><%= t("announcements.edit.title") %></h1>

  <%= render "form", announcement: @announcement %>
</div>
